<% content_for :meta_title, "My experiment - Experiment.rocks" %>

<main class="focus-form">
  <div class="container">
    <div class="row">
      <div class="col form">
        <h1 class="h2 text-intense"><%= start_or_cancel_experiment %> the experiment '<%= @experiment.name %>'</h1>
        <%= render "form", experiment: @experiment %>
      </div>
    </div>
  </div>
</main>
